# CSV Analyst (MCP Server)

Un piccolo server MCP per ispezionare e analizzare CSV in locale.

## Requisiti
- Python 3.10+
- `pip install -r requirements.txt`

## Avvio rapido
```bash
# Windows
./run.ps1

# macOS/Linux
bash run.sh
